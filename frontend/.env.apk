# Dipakai hanya oleh `npm run build:apk` (vite --mode apk).
# APK jalan dari device (capacitor://localhost) tanpa same-origin,
# jadi API harus absolut ke backend prod.
VITE_API_URL=https://mosque.dimassraf.space
